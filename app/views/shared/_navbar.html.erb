<nav class="bg-white border-b py-4 px-5 shadow-sm">
  <div class="container mx-auto flex justify-between items-center">
     

     
     <% if user_signed_in? %>
     <%= link_to "#{current_user.type.capitalize}'s Portal", send("#{current_user.type.downcase}s_root_path") , class: "font-bold text-blue-600 text-xl" %>

      <div class="flex gap-6 items-center">
        <span class="text-gray-700">Hello <strong><%= current_user.full_name %></strong></span> | 
        <%= button_to "Logout", destroy_user_session_path, method: :delete, class: " cursor-pointer inline-flex items-center px-4 py-2 bg-red-600 text-white text-sm font-medium rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500" %>
      </div>
     <% else %>
     <%= link_to "Clinic Portal", root_path, class: "font-bold text-blue-600 text-xl cursor-pointer " %>
       <%= link_to "Login", new_user_session_path, class: "text-blue-600 hover:underline cursor-pointer " %>
     <% end %>
  </div>
</nav>
 